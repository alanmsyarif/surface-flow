[project]
name = "flumen"
version = "0.0.2"
description = "Procedural surface drainage R&D for Blender Geometry Nodes"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
norecursedirs = ["blender"]
