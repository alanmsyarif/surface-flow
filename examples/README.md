# Examples

Binary `.blend` example files are intentionally not generated outside Blender.

Recommended scenes:

- `sphere.blend`: UV sphere radius 1
- `bottle.blend`: closed bottle with shoulder and lip
- `head.blend`: clean manifold head mesh

Run `scripts/smoke_test_blender.py` inside Blender to generate the first test `.blend` automatically.
